import { actionGenerator } from '../../util/reduxHelpers';

// get posts
export const GET_POSTS = actionGenerator(
  'GET_POSTS',
);

// post create
export const CREATE_POST = actionGenerator(
  'CREATE_POST',
);

